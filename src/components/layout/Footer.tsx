import { Copy } from "../common/Copy"

export const Footer = () => {

    return <footer className={["site-footer mobile"].join(" ")}>
        <div className="wrapper">
            <Copy  />
        </div>
    </footer>
}